let dato = prompt()

// Pedira que ingrese un dato hasta que la variable dato sea igual a salir

// se va a repetir siempre y cuando el dato ingresado sea distinto de salir

while (dato != "salir") {
    alert ("El usuario ingreso " +dato)
    dato = prompt ()    
}